---
- name: Remove old Ghostscript version
  package:
    name: ghostscript
    state: absent

- name: Download Ghostscript source
  unarchive:
    src: '{{ ghostscript_download_url }}'
    dest: /tmp
    remote_src: yes
  register: gs_dir

- debug:
    var: gs_dir  